<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/materialGray</item>
        <item name="colorPrimaryDark">@color/materialGrayDark</item>
        <item name="colorAccent">@android:color/holo_blue_dark</item>
        <item name="android:listPreferredItemHeight">60dp</item>
    </style>

    <style name="AppThemeNoToolBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/materialGray</item>
        <item name="colorPrimaryDark">@color/materialGrayDark</item>
        <item name="colorAccent">@android:color/holo_blue_dark</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppCompatTransparent" parent="Theme.AppCompat.Light">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
    </style>
    
    <style name="LoginButton" parent="Theme.AppCompat">
        <item name="colorControlHighlight">@color/cardview_light_background</item>
        <item name="colorButtonNormal">@color/cardview_dark_background</item>
    </style>

</resources>
